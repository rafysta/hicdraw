% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_from_config.R
\name{draw_from_config}
\alias{draw_from_config}
\title{Generate many figures from a configuration table}
\usage{
draw_from_config(
  config_file,
  sample_sheet,
  matrix_dir,
  boundary_dir,
  out_dir,
  gene_file = NULL,
  chrom_sizes = pombe_chrom_sizes,
  depth_target = 1e+08,
  base_width = 22,
  base_height = 16,
  dpi = 200,
  rows = NULL
)
}
\arguments{
\item{config_file}{Path to a \code{.xlsx}, \code{.tsv} or \code{.csv} configuration table.}

\item{sample_sheet}{Path to the sample sheet (\code{.tsv}) or a data.frame with
columns \code{sample_id}, \code{matrix_file}, \code{boundary_file}, \code{total_read}.}

\item{matrix_dir}{Directory holding the matrix files.}

\item{boundary_dir}{Directory holding the boundary files.}

\item{out_dir}{Output directory (created if needed).}

\item{gene_file}{Optional gene model file to add a gene panel.}

\item{chrom_sizes}{Chromosome sizes (default \link{pombe_chrom_sizes}).}

\item{depth_target}{Reference total read count for \code{scale = "depth"}
(default 1e8).}

\item{base_width, base_height}{Saved figure size in cm.}

\item{dpi}{Saved figure resolution.}

\item{rows}{Optional integer vector to restrict which config rows are drawn.}
}
\value{
Invisibly, a character vector of written file paths.
}
\description{
Reads a configuration table (one row per output figure) and a sample sheet,
then draws and saves each requested figure. Modelled on the parameter-file
driven workflow used for the .hic maps, but aware of the hic200 format and
of boundary tracks.
}
\details{
Configuration columns (missing optional columns are tolerated):
\describe{
\item{id}{Row identifier.}
\item{type}{\code{"original"} (default) or \code{"subtraction"} (map1 - map2).}
\item{data_format}{\code{"hic200"} (default) or \code{"hic"}.}
\item{input_id1, input_id2}{\code{sample_id} values referencing the sample sheet.}
\item{normalization}{Only used for \code{.hic} maps.}
\item{resolution}{e.g. \code{"200bp"}.}
\item{chr, start, end}{Region to draw.}
\item{max_distance}{Distance from the diagonal to draw (bp); default region width.}
\item{color_scale_max_value}{Explicit colour-scale maximum.}
\item{color_scale_max_percentile}{Quantile for the colour-scale maximum (default 0.99).}
\item{with_boundary}{\code{TRUE}/\code{FALSE} — stack a boundary-strength panel.}
\item{scale}{\code{"none"} (default) or \code{"depth"} (normalise by total_read).}
\item{output_name}{Output file stem.}
}
}
