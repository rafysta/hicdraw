% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hic_data.R
\name{addHiCdata}
\alias{addHiCdata}
\title{Attach a Hi-C contact map to the object}
\usage{
addHiCdata(
  hic,
  file,
  resolution,
  dataName = "hic1",
  normalization = "ICE",
  format = c("hic", "hic200"),
  chrom_sizes = pombe_chrom_sizes,
  total_read = NA_real_
)
}
\arguments{
\item{hic}{A Hi-C object.}

\item{file}{Path to the map file.}

\item{resolution}{Resolution string, e.g. \code{"200bp"}, \code{"5kb"}.}

\item{dataName}{Name used to refer to this map later.}

\item{normalization}{Normalisation for \code{.hic} (\code{"ICE"} is mapped to
\code{"NONE"}); ignored for \code{hic200}.}

\item{format}{\code{"hic"} or \code{"hic200"}.}

\item{chrom_sizes}{Chromosome sizes for \code{hic200} (default \link{pombe_chrom_sizes}).}

\item{total_read}{Optional total read count for depth normalisation; stored
for later use by \code{\link[=drawHiC]{drawHiC()}} / \code{\link[=draw_from_config]{draw_from_config()}}.}
}
\value{
The updated Hi-C object.
}
\description{
Two input formats are supported:
\itemize{
\item \code{format = "hic"} — a Juicer \code{.hic} file, read on demand through
\code{\link[strawr:straw]{strawr::straw()}} (the classic behaviour).
\item \code{format = "hic200"} — a sparse \verb{bin1 bin2 score} matrix produced by
hic200-cpp. The file is read once with \code{\link[=read_hic200]{read_hic200()}} and cached inside
the object.
}
}
