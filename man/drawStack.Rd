% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{drawStack}
\alias{drawStack}
\title{Stack a Hi-C map with a boundary track (and optional gene track)}
\usage{
drawStack(
  hic,
  hic_name = "hic1",
  boundary_name = "boundary",
  gene = FALSE,
  col = "matlab",
  zmax = 0.99,
  ymax = NULL,
  unit = 1000,
  title = NULL,
  heights = NULL,
  scale = 1,
  rotate = TRUE,
  fixed = TRUE
)
}
\arguments{
\item{hic}{A Hi-C object with a map and a boundary track attached.}

\item{hic_name}{Name of the attached map.}

\item{boundary_name}{Name of the attached boundary track (or \code{NULL}).}

\item{gene}{If \code{TRUE}, add the gene track (requires \code{\link[=addGenedata]{addGenedata()}}).}

\item{col, zmax, ymax, unit, title}{Passed to \code{\link[=drawHiC]{drawHiC()}}.}

\item{heights}{Relative panel heights.}

\item{scale}{Depth-normalisation multiplier for the map.}

\item{rotate, fixed}{Passed to \code{\link[=drawHiC]{drawHiC()}}. Use \code{rotate = FALSE} for a square
map; its x-axis is then kept aligned with the tracks via \code{egg::ggarrange()}.}
}
\value{
A combined ggplot/cowplot object.
}
\description{
Convenience wrapper that draws the map, its boundary-strength track and an
optional gene model, aligned vertically. Requires \code{cowplot}.
}
