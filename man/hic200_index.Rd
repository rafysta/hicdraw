% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_hic200.R
\name{hic200_index}
\alias{hic200_index}
\title{Build a genome-wide bin index for hic200-cpp matrices}
\usage{
hic200_index(chrom_sizes, res = 200L)
}
\arguments{
\item{chrom_sizes}{data.frame with columns \code{chr} and \code{length} (bp), in the
same order used when the matrix was generated.}

\item{res}{Bin size in bp (default 200).}
}
\value{
data.frame with columns \code{chr}, \code{length}, \code{nbin}, \code{bin_offset}.
}
\description{
hic200-cpp stores contacts as \verb{bin1 bin2 score}, where the bin is a
genome-wide, 0-based index at a fixed resolution. Chromosomes are
concatenated in the order given by \code{chrom_sizes}, so the first bin of a
chromosome starts at the cumulative bin count of all preceding chromosomes.
}
\examples{
hic200_index(pombe_chrom_sizes, res = 200)
}
