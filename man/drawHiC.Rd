% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_hic.R
\name{drawHiC}
\alias{drawHiC}
\title{Draw a Hi-C contact map as a rotated (45 degree) triangle}
\usage{
drawHiC(
  hic,
  dataName = "hic1",
  col = "red",
  ymax = NULL,
  zmax = 0.95,
  xaxis = TRUE,
  unit = 1e+06,
  ntick = 3,
  title = NULL,
  ylabel = "",
  scale = 1,
  rotate = TRUE,
  fixed = TRUE
)
}
\arguments{
\item{hic}{A Hi-C object.}

\item{dataName}{Name of the attached map.}

\item{col}{Fill colour, or \code{"matlab"} for the jet-like palette.}

\item{ymax}{Maximum distance from the diagonal to draw, in bp.}

\item{zmax}{Quantile used for the top of the colour scale.}

\item{xaxis}{Draw the x axis?}

\item{unit}{x-axis unit (e.g. 1e3 for kb).}

\item{ntick}{Approximate number of y ticks.}

\item{title, ylabel}{Plot title and strip label.}

\item{scale}{Numeric multiplier applied to contact counts (depth
normalisation). Default 1.}

\item{rotate}{If \code{TRUE} (default) draw the 45-degree rotated triangle
(x = position, y = distance). If \code{FALSE} draw the standard \strong{square}
matrix (x and y are both genomic position).}

\item{fixed}{Only used when \code{rotate = FALSE}. If \code{TRUE} (default) apply
\code{coord_fixed(ratio = 1)} so the square map has a 1:1 x/y aspect. When such a
square map is stacked with tracks, combine them with \code{\link[=drawStack]{drawStack()}} /
\code{\link[=draw_from_config]{draw_from_config()}}, which use \code{egg::ggarrange()} to keep the x-axis widths
aligned (plain \code{cowplot}/\code{patchwork} cannot align fixed-aspect panels).}
}
\value{
A ggplot object.
}
\description{
Works with both \code{.hic} and \code{hic200} maps attached via \code{\link[=addHiCdata]{addHiCdata()}}.
}
